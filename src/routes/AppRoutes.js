import React from "react";

// import router from "../routerLearn/Main";
// import { RouterProvider } from "react-router-dom";

const AppRoutes = () => {
  return (
    <div>
      working....
      {
        // router
      }
    </div>
  );
  // return <RouterProvider router={router} />;
};

export default AppRoutes;
